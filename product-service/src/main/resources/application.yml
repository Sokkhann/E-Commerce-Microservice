server:
  port: 8082

spring:
  application:
    name: product-service

# register service into eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance: # sensitive case must have this
    hostname: localhost
    prefer-ip-address: true
